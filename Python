class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        if nums == []:
            return 0
        cur = nums[0]
        idx = 1
        while cur != nums[-1]:
            if cur != nums[idx]:
                cur = nums[idx]
                idx += 1
            else:
                nums.pop(idx)
        return idx
